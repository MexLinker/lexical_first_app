# Backend environment configuration (example)

# MySQL connection
DB_HOST=your.mysql.host
DB_PORT=3306
DB_USER=your_user
DB_PASS=your_password

# Choose a database name with the lexical table
# For discovery across multiple DBs, leave blank (dev only)
DB_NAME=english_words_db

# Server port
PORT=3001